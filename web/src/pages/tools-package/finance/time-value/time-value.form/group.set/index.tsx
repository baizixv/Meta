import React from 'react'
import { FormItemGroupSetComp } from '../../../components/form-group-set'

export const FormItemGroupSet = ({ titleStyle, formItemStyle }: any) => {
  return <FormItemGroupSetComp {...{ titleStyle, formItemStyle }} />
}
